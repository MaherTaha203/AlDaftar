import { ReportsCatalog } from '@/components/modules/reports';

/** Route /reports — screen S-80 (report catalog, BDR-10 / BDD-009). */
export default function ReportsPage() {
  return <ReportsCatalog />;
}
