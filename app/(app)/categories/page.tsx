import { CategoriesScreen } from '@/components/modules/categories';

/** Route /categories — screen S-60 (categories managed list). */
export default function CategoriesPage() {
  return <CategoriesScreen />;
}
