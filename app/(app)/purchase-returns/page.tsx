import { ReturnsList } from '@/components/modules/purchase-returns';

/** Route /purchase-returns — screen S-30 (purchase returns list). */
export default function PurchaseReturnsPage() {
  return <ReturnsList />;
}
