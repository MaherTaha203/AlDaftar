import { CreditNotesList } from '@/components/modules/credit-notes';

/** Route /credit-notes — «إشعارات دائنة للموردين» list (BDD-011, التسويات tab 1). */
export default function CreditNotesPage() {
  return <CreditNotesList />;
}
