import { CurrenciesScreen } from '@/components/modules/currencies';

/** Route /currencies — screen S-62 (currencies managed list). */
export default function CurrenciesPage() {
  return <CurrenciesScreen />;
}
