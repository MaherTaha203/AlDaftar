import { RefundsList } from '@/components/modules/payment-refunds';

/** Route /payment-refunds — «سندات استرداد الدفعات» list (BDD-011, التسويات tab 2). */
export default function PaymentRefundsPage() {
  return <RefundsList />;
}
