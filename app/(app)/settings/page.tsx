import { SettingsScreen } from '@/components/modules/settings';

/** Route /settings — screen S-70 (company profile + approved constants). */
export default function SettingsPage() {
  return <SettingsScreen />;
}
