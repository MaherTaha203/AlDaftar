import { DashboardScreen } from '@/components/modules/dashboard';

/** Route / — the dashboard home surface (R-01, 01 §8). */
export default function DashboardPage() {
  return <DashboardScreen />;
}
