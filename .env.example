# Supabase connection.
# Real values are added during the Database Implementation phase,
# after the Database Architecture has been approved.
# Never commit populated credentials. Copy this file to `.env.local`.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
