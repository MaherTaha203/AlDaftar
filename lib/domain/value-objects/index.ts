/*
 * Reserved for concrete value objects (defined in the business phases).
 * Value objects defined here must extend ValueObject from lib/domain/common
 * and may depend only on lib/core and lib/domain.
 *
 * Intentionally empty in the Domain Foundation phase.
 */

export {};
