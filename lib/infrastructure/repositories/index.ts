// Generic repository contracts — reusable infrastructure, no business repositories.

export type {
  EntityId,
  IReadRepository,
  IRepository,
  IWriteRepository,
  QueryOptions,
} from './repository';
